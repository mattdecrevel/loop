DATABASE_URL=
DATABASE_URL_UNPOOLED=
SLACK_BOT_TOKEN=
SLACK_SIGNING_SECRET=
LOOP_FALLBACK_CHANNEL_ID=
# Phase 2 — interactivity → GitHub issues
GITHUB_TOKEN=
GITHUB_WEBHOOK_SECRET=
# Phase 3 — cron auth (reminders + digest). Vercel injects the Bearer on prod.
CRON_SECRET=
